@model Data.Entityes.Categoryes

@{
    ViewData["Title"] = "CreateCategory";
}

<h2>CreateCategory</h2>

<h4>Categoryes</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateCategory">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        
            <div class="form-group">
                <label asp-for="TypeDeparmen" class="control-label"></label>
                <input asp-for="TypeDeparmen" class="form-control" />
                <span asp-validation-for="TypeDeparmen" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Norms">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
